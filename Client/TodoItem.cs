public class TodoItem                   // Declares a class named TodoItem.
{
    public string Title { get; set; }   // Declares a field of type string with a getter and a setter.
    public bool IsDone { get; set; }    // Declares a field of type boolean with a getter and a setter.
}